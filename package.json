{
  "name": "neolution.extensions.caching",
  "private": true,
  "workspaces": ["src/*"],
  "scripts": {
    "changeset": "changeset",
    "version-packages": "node scripts/sync-versions.js",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.0",
    "@changesets/changelog-github": "^0.5.0"
  }
}
